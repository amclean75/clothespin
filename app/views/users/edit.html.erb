<h1>Editing <%= @user.username %></h1>

<%= render "form" %>
	<p><%= link_to "Cancel", users_path %></p>
	
	<h3><%= link_to "Close Account", @user, method: :delete, data: {confirm: "Are you sure?"} %></h3>